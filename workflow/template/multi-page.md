# <项目/模块中文名>

> 模板类型：**多页面**（一个项目 · 一组页面骨架 · 派 N 个 single-page 模板）
> 文件：`requirements/<slug>-YYYY-MM-DD/<slug>-multi-req.md`
> 创建时间：YYYY-MM-DD
> 状态：draft / confirmed / archived

## 修订记录

| 日期 | 修订人 | 内容 |
|---|---|---|
| YYYY-MM-DD | 主会话 | 首次采集（按 4 题 Q-Bank） |

## WHY · 这组页面为什么存在
- **价值**：（一句话，N 种角色 N 时间内完成 X 业务）
- **不做会怎样**：（替代方案的痛点）
- **覆盖范围**：覆盖 use case xx ~ xx；**不覆盖** xx（去管理端 / 后续迭代 / 已删除）

## Q1 · 角色 + 高频场景
> 决定默认落地页、首屏重心、是否要"角色变体"区块

- **服务角色**：
  - ⬜ 单一角色（最小集）
  - ⬜ 主角色 + 次角色，同套页面按差异展示（推荐）
  - ⬜ 多角色合并视角

- **角色清单**：
  - 主角色：（如"普通员工 / 月 X 次 / 心智状态"）
  - 次角色：（可选 0~2 个）

- **最高频 2~3 个场景**：
  1. 场景一：
  2. 场景二：
  3. 场景三（可选）：

## Q2 · UC 合并/拆分策略
> 决定页数减法

- **能力重叠 UC**（典型：列表 / 时间轴 / 看板 / 日历 多视图）：
  - 合一页 + 顶部 view 切换：UC-X + UC-Y → <合后页面名>
  - 单独成页的 UC：UC-Z → <页面名>

- **主 + 衍生**（单次/周期 · 单条/批量 · 主商品/变体）：
  - ⬜ 同一表单 + toggle 展开（推荐）
  - ⬜ 拆独立路径（复杂度差异极大时）
  - 具体落位：

## Q3 · 一级导航 + 默认落地页
> 决定页面骨架

- **一级导航项**（推荐 2~3 项 + 右上铃铛通知）：
  - 入口 1：（「我的 X」/「找 X」/「首页仪表盘」）
  - 入口 2：
  - 入口 3（可选）：

- **默认落地页**：
  - 配对建议：
    - 高频"查自己" → 我的 X
    - 高频"搜寻"   → 找 X
    - 并重         → 首页仪表盘

## Q4 · 表单 + 详情 + 轻量功能 的承载形态
> 决定哪些是独立页面、页数最终定下来

- **主要创建/编辑表单**：
  - ⬜ 右侧抽屉（推荐：中等复杂度 + 保留上下文）
  - ⬜ 跳独立页（超复杂 / 要分享链接 / 多步向导）
  - ⬜ 居中弹窗（5 个字段以内）

- **业务对象详情**：
  - ⬜ 跳独立页（推荐：内容多/动作多/需要分享）
  - ⬜ 右侧抽屉（轻浏览 + 少量动作）
  - ⬜ 行内展开（只查看不动作）

- **轻量常用功能**（通知/消息/收藏/快捷）：
  - ⬜ 右上图标 + 下拉面板 + 「查看全部」跳独立页（推荐 to B）
  - ⬜ 只跳独立页
  - ⬜ 只做面板

## Q5 · 画原型前的最后澄清
> Q1~Q4 答完后，**在产出 PAGES 表和 PROTO 之前**再过一遍：还有什么不清楚的、可能影响原型表达的，先列在这里和用户对齐。
> ⚠️ 这一节是最后闸门。**有疑问先确认再开画，不要边画边猜**。

### 常见盲点（自检清单）
- **字段优先级 F1/F2**：列表/详情页的字段哪些必看、哪些折叠，是否已明确？
- **详情页附加区块**：是否要画状态时间线 / 操作日志 / 相关推荐 / 历史变更？
- **筛选默认值 + 排序**：列表页默认筛选 / 默认排序是否已约定？
- **空态 / 极端数据**：是否要在 PROTO 里画 0 条 / 1 条 / 极多 三种？
- **sidebar 底部入口**：管理后台 / 帮助 / 设置 等是否要进 sidebar？
- **跨页字段一致性**：同一字段在多页出现时（如"会议室名+位置"），是否统一展示形式？
- **抽屉 / 弹窗 触发源**：每个叠加层从哪些位置可触发，是否已明确？
- **角色变体**：同一页不同角色看到的字段差异 / 按钮差异，是否已明确？

### 待澄清问题清单
> 自检后真的有疑问的写在这里。一条一行，带"问什么 → 备选答案"，等用户答复后再进 PAGES + PROTO。

| # | 问题 | 备选 | 用户答复 |
|---|---|---|---|
| 1 | … | A / B / C | … |
| 2 | … | … | … |

## PAGES · 最终页面清单
> Q1~Q4 答完后的产出。每一行后续派一个 single-page 模板。

| # | 页面 slug | 中文名 | 形态 | 覆盖 UC | 备注 |
|---|---|---|---|---|---|
| 1 | <slug-1> | <名> | 独立页 / 抽屉 / 面板 | UC-X、UC-Y | |
| 2 | <slug-2> | <名> | | | |
| 3 | <slug-3> | <名> | | | |

**导航 + 跳转骨架**（ASCII，仅画顶层关系）：

```
┌─ 一级导航 ─────────────────┐
│  我的 X  ← 默认落地         │
│  找 X                      │
│              🔔 ← 右上铃铛  │
└────────────────────────────┘
       │
       ├─→ 页面 A ─→ 详情页（独立）─→ 编辑（抽屉）
       │
       ├─→ 页面 B ─→ 创建（抽屉）─→ 详情页
       │
       └─→ 通知面板 ─→ 详情页
                    └─→ 通知中心独立页
```

## PROTO · 原型输出
> Q1~Q4 + PAGES 确定后产出。先画统一框架（sidebar + content-header），再画每一页完整原型。
>
> ⚠️ **PROTO 只是设计沟通用的参考图，不是实现规格**。
> page-agent 装配时**直接引用** `fragment/fragment.html` 作为页面骨架，不照着 ASCII 重写 sidebar / content-header 的结构。
> 原型用于对齐 "菜单项填什么 / 字段怎么放 / 哪些区段出现"，**容器层面由 fragment 接管**：
> - sidebar 的宽度、收起按钮、底部信息位 → fragment 提供，本段只填菜单项内容
> - content-header 的高度、边距、左右槽位 → fragment 提供，本段只决定填哪个变体（列表型 / 详情型）
> - main-area 的滚动行为、自适应 → fragment 提供，本段只填字段落位

### PROTO-1 · 二级导航 sidebar（统一框架，所有页共用）

```
┌─ 二级导航 sidebar ──────────┐
│  <模块名>       [收起 ⇋]    │
│  <副标题占位>                │
│                            │
│  ┌──────────────────────┐  │
│  │     [+ 主 CTA]        │  │  ← 固定 sidebar 顶部
│  └──────────────────────┘  │
│                            │
│  <icon>  <菜单项 1>  ● 默认选中
│  <icon>  <菜单项 2>          │
│  ...（按 PAGES 独立页数填）   │
│                            │
│ ─────────────────────────  │
│  ⚙   管理后台   (有权限时)  │
└────────────────────────────┘
```

**规则**：
- 主 CTA 唯一，固定 sidebar 顶部（不放 content-header）
- 菜单项数量按 PAGES 表的独立页数确定，不展开三级菜单（除非业务必要）
- 通知不进 sidebar（走顶栏铃铛）
- 「管理后台」按权限条件显示

### PROTO-2 · 内容区头部 content-header（两种变体）

#### 变体 A · 列表型
适用：列表 / 卡片 / 时间轴 / 看板 / 通知中心

```
┌─ content-header ─────────────────────────────────────────────┐
│  一级标题                                          [搜索 🔍] │
│  <副标题占位>                                                 │
└──────────────────────────────────────────────────────────────┘
```

#### 变体 B · 详情型
适用：业务对象详情

```
┌─ content-header ─────────────────────────────────────────────┐
│  [← 返回]  一级标题                       · 状态徽标         │
│            <副标题占位：关键摘要>                              │
└──────────────────────────────────────────────────────────────┘
```

**规则**：
- 右上角**禁止画主 CTA**（已被 sidebar 接住）
- 右上角只允许放：搜索 / 视图切换 / 次要操作（导出 / 批量 / 刷新）
- 详情型统一带 `[← 返回]`，标题后跟状态徽标

### PROTO-3 · 每个页面完整原型

> 每个 PAGES 行画一张完整 ASCII（sidebar + 内容区一起），标注 secondary-nav / filter-bar / main-area 内的字段落位 F1/F2。

#### PROTO-3.<n> · <页面中文名>

```
┌─ 二级导航 sidebar ────┬─ 内容区 ─────────────────────────────────────────┐
│  <模块名>   [收起 ⇋]   │  content-header                                  │
│  <副标题占位>           │  ── <一级标题>                    [搜索/视图]    │
│                       │     <副标题占位>                                 │
│   [+ 主 CTA]           │                                                  │
│                       │  secondary-nav (可选)                            │
│  <icon> <菜单项1>  ●  │  ── [tab1] [tab2] [tab3]                        │
│                       │                                                  │
│  <icon> <菜单项2>      │  filter-bar (可选)                              │
│                       │  ── [筛选1] [筛选2] [关键字...]                   │
│                       │                                                  │
│                       │  main-area                                       │
│                       │  ┌─────────────────────────────────────────┐    │
│                       │  │ F1·主要字段                              │    │
│                       │  │ F1·... F2·...                            │    │
│                       │  │            [次CTA] [次CTA]                │    │
│                       │  └─────────────────────────────────────────┘    │
│  ⚙ 管理后台            │                                                  │
└───────────────────────┴──────────────────────────────────────────────────┘
```

**抽屉 / 弹窗 / 面板形态**单独画，标注触发源和叠加方式：

```
┌─ 二级导航 ────────────┬─ 内容区（保持原页）─┬─ 抽屉/面板（覆盖位置）──────┐
│  ...（sidebar 同上）   │                    │ <标题>                  [×] │
│                       │  当前内容保留        │ ──────                      │
│                       │  在叠加层下可见       │ <字段 / 列表项>              │
│                       │  按需禁用            │                            │
│                       │                    │ [次CTA] [次CTA]             │
└───────────────────────┴────────────────────┴─────────────────────────────┘
```

**验收**：
- 每张完整原型与 PAGES 表对应行的 **形态 / UC / 备注** 必须一致
- secondary-nav 和 filter-bar 是可选段，按页面需要决定
- 字段落位用 F1 / F2 标记优先级，**不写具体业务文案**（由 page-agent 装配时按 REQ 填）
- ⚠️ 主 CTA 永远在 sidebar，content-header 不重复画

## EDGE · 项目级约束
- **统一交互规范**（写在这里，单页 brief 不重复）：
  - 越权按钮：主动操作 → 灰 + tooltip；身份无关 → hidden；路由级 → 跳无权页
  - 危险操作：弹窗二次确认（删除 / 取消 / 释放 / 重置）
  - 成功反馈：toast；失败反馈：行内 inline
- **私有 CSS 前缀**：`--<project-prefix>-*`
- **视口**：（仅桌面 1440+ / 自适应 / 仅移动）

> ⚠️ 本节**禁止列具体 cs- 组件清单**。组件选型在单页 brief 和 page-agent 装配阶段处理。

## 派单准备
- 每一行 PAGES 派 **一个 single-page 模板**（`template/single-page.md`），文件落到 `requirements/<slug>-YYYY-MM-DD/<slug>-req.md`
- 抽屉 / 面板形态的"页"也独立产出 single-page brief，PROTO 阶段标注承载形式
- 各 single-page brief confirm 后，再按 single-page 模板的"派单准备"派 **page-agent**
- ⚠️ **跨页禁止参考** —— 每个 single-page brief 独立产出，避免污染（见 memory `feedback_no_baseline_reuse`）
- 多页 brief 可以并行采集（参考 `superpowers:dispatching-parallel-agents`），但 PAGES 表必须先 confirm，避免页数变动后白做
